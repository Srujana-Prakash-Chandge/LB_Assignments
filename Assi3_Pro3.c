#include<stdio.h>
void DisplayEvenFactor(int No)
{
    int i = 0;
    if(iNo <= 0)
    {
        iNo = -iNo;
    }

    for(i = 1; i<= iNo/2 ; i++)
    {
        if(    &&   )
        {
            printf("%d", i);
        }
    }
}

int main(){
    int i Value= 0;
    printf("Enter Number :");
    scanf("%d", &iValue);
    DisplayEvenFactor(iValue);
    return 0;
}